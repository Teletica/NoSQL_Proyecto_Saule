@model IEnumerable<NoSQL_Proyecto_Saule.Models.Proveedor>

@{
    ViewBag.Title = "List";
}

<h2>Lista</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreProveedor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ApellidoUnoProveedor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ApellidoDosProveedor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CompanyProveedor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContactoProveedor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreProveedor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ApellidoUnoProveedor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ApellidoDosProveedor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CompanyProveedor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContactoProveedor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
