@model IEnumerable<NoSQL_Proyecto_Saule.Models.Receta>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcionReceta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fechaReceta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prescriptorReceta)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.firmaPrescriptorReceta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descripcionReceta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fechaReceta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prescriptorReceta)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.firmaPrescriptorReceta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
